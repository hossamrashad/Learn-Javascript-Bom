/*globel console, document, window, alert, prompt */

/*
 * window
 *
 * - window object is the browseer window
 *
 * - window object supported by all browsers مداعم فى جميع المتصفحات
 *
 * - window object has propertise & methods not suported in all browser
 *
 *
 * window.print(); اللى هو اطباع الصفحة
 *
 * */
window.print();
